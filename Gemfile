source "https://rubygems.org"

# This mirrors the exact Jekyll + plugin versions GitHub Pages uses to build
# your site, so what you see locally matches what gets published.
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3+
gem "webrick", "~> 1.8"
